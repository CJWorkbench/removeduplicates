id_name: removeduplicates
name: Deduplicate
category: Clean
description: Delete duplicate rows by comparing values in one or several columns
icon: clean
help_url: modules/clean/deduplicate
parameters:
- id_name: colnames
  type: multicolumn
  name: Find duplicates in
  deprecated_string_storage: false
- id_name: action
  name: "and"
  type: menu
  default: delete
  options:
  - { value: delete, label: 'Delete duplicate rows' }
  - { value: cumcount, label: 'Number duplicates in new column' }
